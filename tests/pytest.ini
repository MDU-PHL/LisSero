# specify default options when running pytest
# here:
# -s: show code print() on the console
# -rf: show extra test summary when test fails
# --doctest-modules: run any doctests if they are available
[pytest]
addopts = -s -rf --doctest-modules
